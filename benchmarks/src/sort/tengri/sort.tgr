// Minimal benchmark program for Tengri.
// Generates array literal via the Makefile (auto-filled with SIZE..1).
// The second line calls sort(xs) to do the work and print result.
//
// Line 1 will be replaced by Makefile to a huge literal like:
// jasa xs : j'i'm = [100000, 99999, ..., 1]
//
// This placeholder stays valid if you want to run tiny quick test by hand:
jasa xs : j'i'm = [5, 1, 3, 2, 4]
sort(xs)